# Experiment 2 — does a longer draft horizon raise acceptance length?

DSpark drafts a whole block of tokens per anchor; `block_size` sets how long that
block is. This experiment asks whether **extending the draft horizon from 7 to 16**
raises the number of tokens the target accepts per verifier call. We fine-tuned our
own block-16 drafter, warm-started from DeepSpec's block-7 checkpoint, and compare
the two under identical tree + markov decoding.

**Verifier:** `Qwen/Qwen3-4B`, greedy (temperature 0, deterministic). At temperature
0 acceptance length is GPU-independent, so the numbers reproduce exactly.

The two drafters are architecturally identical (5 draft layers, hidden 2560,
`markov_rank` 256, same `target_layer_ids` / mask token / confidence head); they
differ **only** in `block_size` (7 vs 16). That makes this a clean block-size
ablation.

Our block-16 model is published at
**https://huggingface.co/shreybirmiwal/Qwen3-4B-DSpark-b16** (public).

## The methods

Four arms run by default (`backbone × corrector × verify`, all tree decoding):

| method | backbone | corrector | note |
|---|---|---|---|
| `dspark_b7.tree` | DSpark-b7 (`deepseek-ai/dspark_qwen3_4b_block7`) | none | markov-off control |
| `dspark_b7.markov.tree` | DSpark-b7 | own markov head | **headline** |
| `dspark_b16.tree` | DSpark-b16 (`shreybirmiwal/Qwen3-4B-DSpark-b16`) | none | markov-off control |
| `dspark_b16.markov.tree` | DSpark-b16 | own markov head | **headline** |

The two `*.markov.tree` arms are the headline comparison. The matched `*.tree`
controls turn the corrector off, so the block-size effect is readable in isolation
from the corrector's contribution. The block-size effect is the **within-arm delta**
between b7 and b16: `dspark_b16.markov.tree − dspark_b7.markov.tree` (headline) and
`dspark_b16.tree − dspark_b7.tree` (control). The claim predicts both > 0.

**Caveat — the benefit is task-dependent.** A longer horizon only helps on tasks
whose completions actually reach deep tree positions. The prior phase's notes
(`old-experiments/experiments/RESULTS.md` §7) record that alpaca-style prompts never
reach depth 8, so depths 9–16 are capacity that task never uses. Our task mix
(gsm8k / humaneval / mt-bench) includes chat (mt-bench), which is the weakest case
for a longer draft horizon; expect the block-16 advantage to concentrate on the
structured math/code tasks, not to be uniform across the mix.

## The model + training recipe

`shreybirmiwal/Qwen3-4B-DSpark-b16` was **warm-started** from
`deepseek-ai/dspark_qwen3_4b_block7` and fine-tuned with
[DeepSpec](https://github.com/deepseek-ai/DeepSpec) (MIT). Warm-starting across
block sizes is valid because `block_size` only controls how many mask tokens are
appended per anchor during training — no parameter shape changes — so the block-7
weights load into the block-16 model with `load_state_dict(strict=True)`.

Recipe (shipped `_best` checkpoint; `training/dspark_block16_qwen3_4b.py`):

| knob | value |
|---|---|
| block_size | 7 → **16** |
| learning rate | 1e-4, warmup ratio 0.03 |
| max train steps | 600 |
| global batch size | 32 (local 1 × grad-accum) |
| num_anchors | 32 (cut from DeepSpec's 512 to fit a 24GB A10G) |
| markov_rank / head | 256, vanilla; jointly trained |
| confidence head | on (`alpha` 1.0, computed with markov) |
| loss | `0.1·CE + 0.9·TV + 1.0·conf`, position decay γ=4.0 |
| precision | bf16 |
| data | `mlabonne/open-perfectblend` subset: 2,400 downloaded → 2,280 after the 5% eval split, responses regenerated by the target; `max_length` 768 |

Full pipeline and rationale: **`training/README.md`**.

## Tunables (constants at the top of `modal_benchmark.py`)

| knob | default | notes |
|---|---|---|
| `TARGET` | `Qwen/Qwen3-4B` | verifier |
| `BACKBONES` | dspark_b7, dspark_b16 | each `{name, model_id, kind}`; block_size intrinsic to each checkpoint |
| `BLOCK16_MODEL_ID` | `shreybirmiwal/Qwen3-4B-DSpark-b16` | our published checkpoint, swappable |
| `METHODS` | the four arms above | any `backbone × corrector × verify` cell |
| `TASKS` | gsm8k:8, humaneval:8, mt-bench:8 | `(dataset, max_samples)` |
| `TREE_BUDGET` | 64 | |
| `TEMPERATURE` | 0.0 | tree build is greedy top-k regardless |
| `MAX_NEW_TOKENS` | 512 | |
| `SEED` | 0 | |
| `MEASURE_PER_DEPTH` | True | |
| `DEPTH_REPORT_LIMIT` | **16** | must reach the b16 horizon or its advantage is invisible |
| `GPU` / `TIMEOUT_SECONDS` | A100-40GB / 1h | |

## How to run

### 1. Train (optional — the model is already published)

```bash
pip install modal
modal setup                                          # one-time auth
cd experiment2-block16/training
modal run --detach modal_train.py::pipeline --exp-suffix _best
```

A10G, 2400 rows → the `_best` checkpoint. `--detach` survives a dropped connection.
See `training/README.md` for the H100 `_bigdata` arm and `publish`.

### 2. Benchmark

```bash
cd experiment2-block16
modal run modal_benchmark.py                         # add --detach if desired
```

### 3. Charts

```bash
cd experiment2-block16/results
python make_charts.py [path/to/summary.json]         # default: ./summary.json
```

Writes three PNGs next to the JSON (matplotlib Agg, no display needed):

| file | what it shows |
|---|---|
| `block_size.png` | headline dumbbell: mean acceptance length b7 → b16, per arm |
| `acceptance_by_method.png` | mean acceptance length, all four methods, grouped by dataset |
| `per_depth_accept.png` | conditional accept rate vs tree depth; the b7 curves end at their block-7 horizon, the b16 curves run to 16 |

## Where results land

The benchmark writes `summary.json`:
- on the shared `ddtree-results` volume at **`/results/block16/summary.json`**
  (namespaced so it does not clobber experiment 1's `/results/summary.json`), and
- downloaded to **`experiment2-block16/results/summary.json`**.

`summary.json` shape:
```json
{
  "config":  { "target": "...", "depth_report_limit": 16,
               "backbones": { "dspark_b16": {"model_id": "...", "kind": "dspark", "block_size": 16}, "...": "..." },
               "methods":   { "dspark_b16.markov.tree": {"backbone": "...", "corrector": "...", "verify": "tree"}, "...": "..." } },
  "results": { "gsm8k": { "dspark_b16.markov.tree": {"mean_accept": ..., "rounds": ..., "per_depth_accept": {"1": ..., "16": ...}}, "...": "..." } },
  "block_size": { "markov.tree": { "small_block": 7, "large_block": 16,
                                   "per_dataset": {"gsm8k": {"small": ..., "large": ..., "delta": ..., "pct_change": ...}},
                                   "overall": {"small": ..., "large": ..., "delta": ..., "pct_change": ...} } }
}
```
(`transfer` and the corrector-fit probe from experiment 1 are gone — the rollup here
is `block_size`.)

## Known gotcha: RoPE base across transformers majors

Both DSpark configs declare RoPE the transformers-v5 way — a nested
`rope_parameters: {rope_theta: 1000000}` with **no top-level `rope_theta`**. The
pinned image uses transformers 4.57.1, whose `Qwen3Config` knows nothing about that
field: it silently falls back to its own default `rope_theta=10000.0` while these
drafters were trained at 1000000.0. Nothing raises — the drafts just get quietly
worse on both arms, which would look like a plausible (wrong) result.

**Mitigation (implemented in `DDTree/run_experiment.py`):**
- `load_config()` reads the nested `rope_parameters["rope_theta"]` and copies it to
  top-level `config.rope_theta`, then passes that config into `from_pretrained`.
- A defense-in-depth guard recovers the base the model will *actually* use from the
  rotary embedding's `inv_freq` buffer and asserts it equals 1e6 (raises otherwise),
  so a silent regression cannot pass even if the normalization is later removed.
- A matching guard asserts each loaded `block_size` is 7 / 16 — a failed block_size
  transfer would collapse the one axis under test.

Do not delete these branches while the image pins transformers < 5.

## Pinned environment

| component | version / source |
|---|---|
| base image | `nvidia/cuda:12.4.1-devel-ubuntu22.04`, Python 3.11 |
| torch | `2.5.1` (cu124) |
| flash-attn | `2.7.4.post1` prebuilt wheel (cu12 / torch2.5 / cxx11abiFALSE / cp311) |
| transformers | `4.57.1` (≥4.51 for Qwen3, ≥4.56 for the `dtype=` from_pretrained kwarg) |
| datasets | `3.6.0` |

`flash_attn` is mandatory (draft models use `flash_attention_2`). The `huggingface`
Modal secret is attached; the models and datasets used are public. HF downloads cache
in the `ddtree-hf-cache` volume. Charts need only `matplotlib` locally.
